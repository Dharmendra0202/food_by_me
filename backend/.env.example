PORT=5001
JWT_SECRET=foodbyme_secret_key_2026
NODE_ENV=development

# Default country code used when user enters a number without + prefix
# Example: if DEFAULT_COUNTRY_CODE=+91 and user enters 9876543210,
# backend will normalize it to +919876543210
DEFAULT_COUNTRY_CODE=+91

# Twilio Verify (required for real SMS OTP delivery)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=

# For local development only. Keep false for real OTP flow.
ALLOW_DEV_OTP_FALLBACK=false
